<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nmcarrol_travelcalc"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="nmcarrol Travel Calc"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="nmcarrol Travel Calc" >
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Testing"
            android:label="@string/title_activity_testing" >
        </activity>
        <activity
            android:name=".ClaimEditor"
            android:label="Claim Editor" >
        </activity>
        <activity
            android:name=".ExpenseView"
            android:label="Expenses" >
        </activity>
        <activity
            android:name=".ExpenseEditor"
            android:label="Expense Editor" >
        </activity>
        <activity
            android:name=".ClaimSummary"
            android:label="Summary" >
        </activity>
    </application>

</manifest>
